{
  "name": "food-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "cd client && npx expo start",
    "server": "cd server && node server.js"
  },
  "dependencies": {
    "sequelize": "^6.37.8"
  }
}
